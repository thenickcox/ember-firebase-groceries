//test('models are valid', function(){
  //var store = window.App.__container__.lookup('store:main');
  //item = store.createRecord({ title: 'carrots', isBought: false, quantity: 1 });
  //quantity = item.get('quantity');
  //equal(quantity, item.get('quantity'), 'did not match');
//});
